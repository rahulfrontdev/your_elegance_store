# Production (used by `vite build` / deploy)
VITE_API_BASE_URL=https://yourelegancestore.com/api

# Local development — copy to `.env.local` (already gitignored):
# VITE_API_BASE_URL=http://localhost:8000/api
# VITE_MEDIA_ORIGIN=http://localhost:8000
